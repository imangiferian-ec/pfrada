{% extends 'base.html.twig' %}

{% block body %}
    <h1>Clearance Fee</h1><hr>

    <table class="table">
        <tbody>
            <tr>
                <th>Clearance Application Purpose</th>
                <td>{{ clearance_fee.clearancePurpose }}</td>
            </tr>
            <tr>
                <th>Voter's Fee</th>
                <td>{{ clearance_fee.voterFeeAmount }}</td>
            </tr>
            <tr>
                <th>Non-voter's Fee</th>
                <td>{{ clearance_fee.nonVoterFeeAmount }}</td>
            </tr>
            <tr>
                <th>Is Active</th>
                <td>{{ clearance_fee.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('clearance_fee_index') }}">back to list</a>

    <a href="{{ path('clearance_fee_edit', {'id': clearance_fee.id}) }}">edit</a>

{% endblock %}
